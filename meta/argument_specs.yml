---
argument_specs:
  main:
    short_description: Install yay and AUR packages
    options:
      install:
        type: bool
        default: true
        description: Set to false to uninstall yay
      aur_packages:
        type: list
        elements: str
        default: []
        description: List of AUR packages to install after yay is set up
